<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>REM Average</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* 변경된 스타일 */
        .navbar {
            background-color: #f8f9fa; /* 이전과 동일한 색상 */
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }
        .navbar-brand {
            color: #333; /* 이전과 동일한 색상 */
        }
        .navbar-nav .nav-link {
            color: #333; /* 이전과 동일한 색상 */
        }
        .navbar-nav .nav-link:hover {
            color: #007bff;
        }

        .card {
            margin-top: 20px;
        }
        .card-header {
            background-color: #007bff;
            color: #ffffff;
        }
        .card-body {
            background-color: #f8f9fa;
        }
        .total-average {
            font-weight: bold;
            background-color: #c3e6cb; /* 연두색 */
            padding: 10px;
            margin-top: 20px;
            border-radius: 5px;
            text-align: center;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Sleep Tracker</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/api/getStoredData">수면 기록보기</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/api/getRemData">렘 수면 기록보기</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/timeinfo">알람 정보 보기</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/">알람 시간 입력하기</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <h1>REM Sleep Average</h1>
        <div>
            <% if (remData.length > 0) { %>
                <div class="total-average">
                    Total Avg Period: <%= totalAvg %>
                </div>
                <% remData.forEach(function(data) { %>
                    <div class="card">
                        <div class="card-header">
                            Date: <%= data.dateTime.split('T')[0] %>
                        </div>
                        <div class="card-body">
                            <p class="card-text">Average Period: <%= data.avgPeriod %></p>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No REM sleep data available!</p>
            <% } %>
        </div>
    </div>
</body>
</html>
