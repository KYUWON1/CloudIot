<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stored Sleep Data</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* 변경된 스타일 */
        .navbar {
            background-color: #f8f9fa; /* 이전과 동일한 색상 */
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }
        .navbar-brand {
            color: #333; /* 이전과 동일한 색상 */
        }
        .navbar-nav .nav-link {
            color: #333; /* 이전과 동일한 색상 */
        }
        .navbar-nav .nav-link:hover {
            color: #007bff;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        table, th, td {
            border: 1px solid #dee2e6;
        }
        th, td {
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #007bff;
            color: #ffffff;
        }
        tbody tr:nth-child(even) {
            background-color: #c3e6cb; /* 연두색 변경 */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light"> <!-- 상단바 배경색 변경 -->
        <a class="navbar-brand" href="/">Sleep Tracker</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/api/getStoredData">수면 기록보기</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/api/getRemData">렘 수면 기록보기</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/timeinfo">알람 정보 보기</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/">알람 시간 입력하기</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <h1 class="mt-5">Stored Sleep Data</h1>
        <table class="table mt-3">
            <thead class="thead-dark">
                <tr>
                    <th>Device ID</th>
                    <th>Date of Sleep</th>
                    <th>Total Time in Bed</th>
                    <th>Minutes Asleep</th>
                    <th>Minutes Awake</th>
                </tr>
            </thead>
            <tbody>
                <% result.forEach(sleepData => { %>
                    <tr>
                        <td><%= sleepData.deviceId %></td>
                        <td><%= sleepData.sleepDate %></td>
                        <td><%= sleepData.asleepTotal %> minutes</td>
                        <td><%= sleepData.asleepMin %> minutes</td>
                        <td><%= sleepData.awakeMin %> minutes</td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html>
